function [optimal_index] = minimax(game_status, a, b, turn)
    % should get the index of the best move
    % first just get best move
    legal_positions = get_bot_positions(game_status, turn);
    for i=1:size(legal_positions, 3) % iterate through all legal moves
        
    end
end